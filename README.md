# Module 1 - Week 2 Day 2 Homework

This version follows the supplied PDF without redesigning the classroom project.

## Included PDF changes

- Embedded CSS moved to `styles.css`.
- Stylesheet linked on all three HTML pages.
- Navigation links work and `active` is on the current page.
- `activities` and `contact` body IDs added.
- Header `<img>` removed from Activities and Contact; CSS background images used.
- Partner text replaced with image elements.
- Activities table CSS copied from the PDF values.
- Contact `<main>` uses the provided `contact-main.txt` content.
- Contact form styled using contextual selectors, `:first-child`, and the required colors.
